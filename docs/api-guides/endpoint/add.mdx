---
title: 'Add data to a table'
description: 'Example code for adding data to a table using the table API endpoint.'
---

## Overview

`table.add()` is used to add data to a table using the table API endpoint.

<CardGroup cols={1}>
  <Card
    title="Add API example"
    icon="code"
    href="https://github.com/Ahen-Studio/skypy-db/blob/main/examples/basic_implementation/example_without_a_custom_config.py"
  >
    Example code for adding data to a table using the table API endpoint.
  </Card>
</CardGroup>

## Usage

```python
import skypydb
from skypydb.errors import TableAlreadyExistsError

# setup skypydb client.
client = skypydb.Client(path="./data/skypy.db")

# Create table. get_table, delete_table are also available.
try:
    table = client.create_table("all-my-documents")
except TableAlreadyExistsError:
    # Tables already exist, that's fine
    pass

# Retrieve the table before adding any data.
table = client.get_table("all-my-documents")

# Add data to the table.
table.add(
    title=["document"],
    user_id=["user123"],
    content=["this is a document"],
    id=["auto"]# ids are automatically created by the backend
)
```
